  /*prg to find area of a triangle using sub program*/
  #include<stdio.h>
  #include<conio.h>
  float area(float,float);
  void main()
    {
      float ar,b,h;
      clrscr();
      printf("\n Enter base and height");
      scanf("%f%f",&b,&h);
      ar=area(b,h); //calling function
      printf("\n Area of a traingle =%f",ar);
      getch();

    }
    float area(float x,float y)  //called function
      {
	float ar;
	ar=0.5*x*y;
	return(ar);
      }