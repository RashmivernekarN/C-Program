//prg to find the biggest of two numbers using sub program
#include<stdio.h>
#include<conio.h>
int big(int,int); //prototype declaration
void main()
  {
    int a,b,lar;  //local variable
    clrscr();
    printf("\n Enter two nums");
    scanf("%d%d",&a,&b);
    lar=big(a,b); //function call
    printf("\n Biggest of 2 nums=%d",lar);
    getch();
  }
  int big (int x,int y)         //formal variables
    {
     if(x>y)
       return (x);
     else
       return(y);
    }